# Codex Development Instructions

## プロジェクト

このリポジトリは、Genesの会社・サービス紹介Webサイトです。

現在はHTMLとCSSを中心とした静的サイトです。
指示がない限り、Next.js、React、Vueなどへの移行は行わないでください。

## 作業開始前

作業前に、必ず以下を確認してください。

1. `README.md`
2. `docs/requirements.md`
3. `docs/architecture.md`
4. `tasks/current-task.md`
5. 現在のGitブランチと変更状態

## Gitルール

- `main`ブランチを直接変更しない
- 作業用ブランチ上でのみファイルを変更する
- 作業開始前に現在のブランチを確認する
- 勝手にmainへマージしない
- 勝手に本番デプロイしない
- force pushを行わない
- 指示がない限り、GitHubへのpushは実行しない

## 変更ルール

- 指示された範囲だけを変更する
- 指示のない大規模なデザイン変更を行わない
- 既存の文章を勝手に削除しない
- 既存の問い合わせ導線を勝手に変更しない
- ファイルを削除する前に確認する
- 指示がない限り、ファイル名やURL構造を変更しない
- `robots.txt`と`sitemap.xml`は、SEO関連の指示がある場合のみ変更する
- 現在のレスポンシブ表示を維持する
- 不明な点を推測して大規模に実装しない

## セキュリティ

- APIキー、パスワード、秘密鍵をコードに記載しない
- `.env`系ファイルをGitへ追加しない
- 顧客の個人情報をリポジトリに保存しない
- 外部サービスへの送信や公開操作は、明示的な指示がある場合だけ行う

## iCloudバックアップルール

ファイルを変更する前に、以下のコマンドを実行する。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ".\scripts\backup-to-icloud.ps1" -Stage before

## 作業後の確認

変更後は以下を確認してください。

- HTMLのタグ構造が壊れていないか
- CSSの閉じ括弧が不足していないか
- PCとスマートフォンの表示を大きく崩していないか
- リンク先やボタンの動作を壊していないか
- 指示していないファイルを変更していないか
- 秘密情報が差分に含まれていないか

## 作業完了時の報告

以下を日本語で報告してください。

1. 変更したファイル
2. 変更した内容
3. 変更した理由
4. 確認した項目
5. 残っている問題
6. 人間による確認が必要な箇所

勝手にmainへのマージや本番公開は行わないでください。